apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-web-content
  namespace: demo
data:
  index.html: |
    <!doctype html>
    <html>
      <head><meta charset="utf-8"><title>Argo CD Demo</title></head>
      <body style="font-family: sans-serif; text-align:center; padding-top:40px">
        <h1>🎉 Hello from Argo CD on Rancher Desktop!</h1>
        <p>Commit을 바꾸면 여기 내용이 자동 반영됩니다.</p>
      </body>
    </html>
